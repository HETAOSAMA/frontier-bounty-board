import { BOUNTY_CONFIG_KEYS } from "../../shared/bounty-config";

// Sui System Objects
export const CLOCK_OBJECT_ID = "0x6";

export const TENANT = process.env.TENANT || "dev";
export const BOUNTY_ENV_KEYS = BOUNTY_CONFIG_KEYS;
